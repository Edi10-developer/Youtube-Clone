import styled from 'styled-components';

const VideoDiv = styled.div`
        padding: 1rem;
        width: 57%;
        min-height: 40rem;

        iframe{
            width: 100%;
            height: 34em;
        }
`;

export { VideoDiv };